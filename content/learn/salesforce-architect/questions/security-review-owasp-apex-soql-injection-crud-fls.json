{
  "quizName": "salesforce-architect:lesson:security-review-owasp-apex-soql-injection-crud-fls",
  "title": "Lesson 44: Security Review: OWASP for Apex, SOQL Injection, and Enforcing CRUD and FLS",
  "description": "How a security review reads Apex: the two shapes of SOQL injection and the controls that close them, why with sharing is not access control, and the tools that make CRUD and FLS enforcement verifiable.",
  "questions": [
    {
      "question": "Per the lesson “Security Review: OWASP for Apex, SOQL Injection, and Enforcing CRUD and FLS”, a class declares with sharing and builds dynamic SOQL by concatenating a user-supplied field name and a user-supplied WHERE value. What does with sharing actually do here?",
      "options": [
        "It enforces record-level sharing only, so it neither stops the field name injection nor enforces CRUD and FLS.",
        "It blocks both injections, because sharing prevents a user from querying fields their profile hides.",
        "It forces the class to use WITH SECURITY_ENFORCED, which the platform adds to the query automatically.",
        "It enforces CRUD and FLS but leaves record visibility to the org-wide default."
      ],
      "correct_answer_index": 0,
      "explanation": "Object permissions, field-level security, and sharing are three separate layers, and the sharing keyword only ever enforces the third one."
    },
    {
      "question": "Which statement best reflects the lesson’s guidance?",
      "options": [
        "String.escapeSingleQuotes closes both the value case and the field name case, which makes it the general fix.",
        "A bind variable in the WHERE clause also protects a field name that the user supplied.",
        "Because a bind variable treats a value as data, also escaping that value with escapeSingleQuotes inside the bind improves safety.",
        "A bind variable cannot protect a field or object name, because a name is syntax rather than a value, so user-supplied names must be compared against an allowlist."
      ],
      "correct_answer_index": 3,
      "explanation": "Bind variables and queryWithBinds close value injection, an allowlist closes name injection, escaping is a narrow fallback for quoted literals, and typecasting closes the numeric case."
    },
    {
      "question": "Which conclusion does the lesson support?",
      "options": [
        "WITH USER_MODE raises on missing access, which makes it the graceful-degradation option for records arriving from an external payload.",
        "Security.stripInaccessible removes the fields the running user cannot access and returns the records in an SObjectAccessDecision, so the caller has to check getRemovedFields or getModifiedIndexes rather than ignore the result.",
        "WITH SECURITY_ENFORCED is the modern replacement for WITH USER_MODE at API version 67.0 and later.",
        "Apex tests prove security because they run as the user the component renders for."
      ],
      "correct_answer_index": 1,
      "explanation": "stripInaccessible strips rather than raises, which is why its return value has to be inspected; tests run as a broadly permissioned user, so a passing suite proves the code path, not the restriction."
    }
  ]
}
