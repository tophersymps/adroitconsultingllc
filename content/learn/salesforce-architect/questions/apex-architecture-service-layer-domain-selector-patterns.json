{
  "quizName": "salesforce-architect:lesson:apex-architecture-service-layer-domain-selector-patterns",
  "title": "Lesson 31: Apex Architecture: Service Layer, Domain, and Selector Patterns",
  "description": "How to organize Apex so a requirement change touches one class: a service layer that orchestrates processes, a domain layer that owns object behavior, and a selector layer that owns every SOQL query.",
  "questions": [
    {
      "question": "Per the lesson “Apex Architecture: Service Layer, Domain, and Selector Patterns”, a lending org needs a runCreditChecks operation callable from a Lightning component, a batch job, and a REST endpoint, while the credit scoring math belongs to the Account object. Where does each piece live?",
      "options": [
        "The service layer exposes runCreditChecks as one method per process that every entry point calls, and the scoring math lives in an Accounts domain class.",
        "The trigger owns runCreditChecks, because a trigger is the only place the platform guarantees the logic runs for every caller.",
        "The selector class owns runCreditChecks, since it already owns everything that queries the Account object.",
        "The batch class owns runCreditChecks, and the Lightning component and REST endpoint call that batch class directly."
      ],
      "correct_answer_index": 0,
      "explanation": "The service layer is the entry point for business processes, so every external caller invokes one service method, and object behavior belongs in the domain class rather than in the entry points."
    },
    {
      "question": "Which statement best reflects the lesson’s guidance?",
      "options": [
        "A data load that inserts 10,000 accounts is safe as long as the domain logic queries once per account, because triggers run once per record.",
        "Because a batch class and a REST endpoint cannot call into a trigger, the same business rule should be re-implemented inside each entry point.",
        "Because triggers fire in bulk, per-record work such as one query per account blows the governor budget, so the selector takes set-based criteria and the domain handles the whole collection in one pass.",
        "Queries should stay inline near the code that needs them, since a selector class adds a layer without reducing the number of queries a transaction issues."
      ],
      "correct_answer_index": 2,
      "explanation": "Triggers are bulk by default, so a query per record spends the shared governor budget in proportion to batch size; a selector that accepts a collection and queries once is what keeps the domain logic safe."
    },
    {
      "question": "Which conclusion does the lesson support?",
      "options": [
        "Full service, domain, and selector stacks should be applied to every org, including small data-centric apps that could be delivered declaratively.",
        "When a requirement changes, a correctly layered solution touches one class, because every SOQL statement lives in the selector, every object rule in the domain, and every process in the service.",
        "A service method should contain the query, the calculation, and the DML inline so a reviewer can read the whole process in one place.",
        "Triggers are the right home for nontrivial logic because they cannot inherit, which keeps the logic close to the record change."
      ],
      "correct_answer_index": 1,
      "explanation": "Separation of concerns is the payoff: the service orchestrates, the domain owns behavior, and the selector owns queries, so a changed rule or a changed schema touches exactly one class."
    }
  ]
}
