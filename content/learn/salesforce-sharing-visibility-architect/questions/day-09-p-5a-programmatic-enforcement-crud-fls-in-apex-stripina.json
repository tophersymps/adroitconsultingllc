{
  "quizName": "salesforce-sharing-visibility-architect:lesson:day-09-p-5a-programmatic-enforcement-crud-fls-in-apex-stripina",
  "title": "Lesson 9 - Programmatic Enforcement: CRUD/FLS in Apex, stripInaccessible, without sharing",
  "description": "Given a scenario, recommend the appropriate programmatic solution to ensure security settings (CRUD and FLS) are enforced.",
  "questions": [
    {
      "question": "A developer must return Case records to a Lightning component but hide any fields the calling user cannot read. Which is the correct pattern?",
      "options": [
        "Return the full records and let the UI hide fields",
        "Use Security.stripInaccessible(AccessType.READABLE, cases) before returning",
        "Run the query in a 'without sharing' class",
        "Turn the fields read-only"
      ],
      "correct_answer_index": 1,
      "explanation": "stripInaccessible removes unreadable fields per the running user's FLS before the data reaches the client. Returning full records leaks them, 'without sharing' ignores record sharing, and UI hiding is not security."
    },
    {
      "question": "Why does WITH SECURITY_ENFORCED sometimes throw an exception for a query that runs fine without it?",
      "options": [
        "The object has too many fields",
        "A selected field or object is not readable by the running user, and the enforcement is all-or-nothing",
        "The query contains a formula field",
        "The class is 'with sharing'"
      ],
      "correct_answer_index": 1,
      "explanation": "WITH SECURITY_ENFORCED is strict: any inaccessible field in the SELECT list raises an error rather than returning partial data. This is expected, intended behavior, not a bug."
    },
    {
      "question": "Which is the recommended way to guard an insert from an inbound form so users cannot write fields they lack FLS for?",
      "options": [
        "Insert the raw payload",
        "Security.stripInaccessible(AccessType.CREATABLE, payload) before insert",
        "Make the class 'without sharing'",
        "Add the fields to a page layout"
      ],
      "correct_answer_index": 1,
      "explanation": "Strip CREATABLE fields before DML so only fields the user is allowed to create/update are persisted, closing the mass-assignment FLS gap."
    }
  ]
}
