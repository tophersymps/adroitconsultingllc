{
  "quizName": "salesforce-sharing-visibility-architect:lesson:day-06-p-3b-ui-hiding-vs-real-security-defense-in-depth-field",
  "title": "Lesson 6 - UI Hiding vs Real Security (Defense in Depth), Field Accessibility in Apex",
  "description": "Given a scenario, determine whether UI hiding is sufficient and how to enforce field accessibility programmatically (defense in depth).",
  "questions": [
    {
      "question": "A developer writes a Lightning action that returns Account records with a Compensation field. Managers can see it, reps cannot. What is the minimal robust fix?",
      "options": [
        "Remove the field from the page layout",
        "Add WITH SECURITY_ENFORCED to the query or strip inaccessible fields before returning",
        "Make the class 'without sharing'",
        "Set the OWD to Private"
      ],
      "correct_answer_index": 1,
      "explanation": "WITH SECURITY_ENFORCED / stripInaccessible make the code honor FLS for the calling user, so reps receive the field stripped. Layouts and OWD do not make Apex honor field visibility."
    },
    {
      "question": "Which statement about Apex sharing and field-level security is TRUE?",
      "options": [
        "A 'without sharing' class automatically enforces field-level security",
        "Apex in user mode must explicitly check field accessibility because SOQL does not enforce FLS by default",
        "WITH SECURITY_ENFORCED returns hidden fields but marks them read-only",
        "Field accessibility cannot be checked in Apex"
      ],
      "correct_answer_index": 1,
      "explanation": "Plain SOQL does not filter hidden fields; the developer must add WITH SECURITY_ENFORCED or Schema accessibility checks. System mode does not auto-enforce FLS, and WITH SECURITY_ENFORCED errors rather than silently returning hidden fields."
    },
    {
      "question": "What is the correct layered approach to protect a sensitive field end-to-end?",
      "options": [
        "Hide it in FLS and also validate field accessibility in Apex, plus keep layouts consistent",
        "Only remove it from the page layout",
        "Only rely on the org-wide default",
        "Set the field to read-only in FLS only"
      ],
      "correct_answer_index": 0,
      "explanation": "Defense in depth pairs FLS (hide) with programmatic enforcement (WITH SECURITY_ENFORCED / accessibility checks) and UI consistency. Any single layer alone leaves a leak surface."
    }
  ]
}
