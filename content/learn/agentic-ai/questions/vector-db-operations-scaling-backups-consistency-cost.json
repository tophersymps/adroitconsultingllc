{
  "quizName": "agentic-ai:lesson:vector-db-operations-scaling-backups-consistency-cost",
  "title": "Lesson 45: Vector DB Operations: Scaling, Backups, Consistency, and Cost",
  "description": "Operating a vector database in production: memory as the scaling variable, the backup and restore story for a derived index, the consistency model you must declare, and the cost curve that follows the memory.",
  "questions": [
    {
      "question": "Per the lesson \"Vector DB Operations: Scaling, Backups, Consistency, and Cost\", what is the real variable that decides whether a pgvector HNSW index is fast?",
      "options": [
        "The number of shards the collection is split across",
        "Whether the index working set fits in memory, because HNSW traversal is a chain of random accesses and disk reads degrade latency by an order of magnitude or more",
        "The PostgreSQL connection pool size",
        "The ANN index type chosen at creation"
      ],
      "correct_answer_index": 1,
      "explanation": "The lesson states that HNSW search is a chain of random accesses through a graph and that the index must live in memory to be fast; when it spills to disk, query latency jumps 10 to 100 times. Row count is a proxy; memory is the variable."
    },
    {
      "question": "The lesson says a backup can meet the database RPO and still fail the retrieval RPO. What does that mean?",
      "options": [
        "The database restore succeeded but the indexes were rebuilt with different ANN parameters",
        "Rows were restored cleanly, but the retrieval system was not restored to a known semantic state, so search returns old chunks or mixes embedding model versions",
        "The physical backup was taken before the last write was committed",
        "The restore ran faster than the recovery time objective allowed"
      ],
      "correct_answer_index": 1,
      "explanation": "The lesson's example: you restore the database cleanly, the application comes back, but support search returns old policy chunks and mixes two embedding model versions in the same result set. The backup met the database recovery point but failed the retrieval recovery point."
    },
    {
      "question": "The lesson argues that most teams have never declared a consistency model for their RAG pipeline. What is the stated consequence of that?",
      "options": [
        "The system is strongly consistent by default because the vector index updates synchronously",
        "The system is eventually consistent, implicitly, with a staleness bound nobody measured and no monitoring to detect when the actual window exceeds the intended one",
        "The inconsistency window is zero because the source of truth is the vector database",
        "Consistency only matters for relational data, not for vector search"
      ],
      "correct_answer_index": 1,
      "explanation": "The lesson says the absence of a declaration is not the absence of a model; it is an implicit model: your system is eventually consistent, designed by nobody, with a staleness bound nobody measured and no monitoring to catch when the real window exceeds the intended one."
    }
  ]
}