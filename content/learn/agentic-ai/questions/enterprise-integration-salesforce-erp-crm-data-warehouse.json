{
  "quizName": "agentic-ai:lesson:enterprise-integration-salesforce-erp-crm-data-warehouse",
  "title": "Lesson 44: Enterprise Integration: Connecting Agents to Salesforce, ERP, CRM, and Data Warehouses",
  "description": "Separating the read path from the write path, the three shapes of enterprise connection, governed data-warehouse reads, safe CRM/ERP writes, and identity as the security boundary.",
  "questions": [
    {
      "question": "Per the lesson \"Enterprise Integration: Connecting Agents to Salesforce, ERP, CRM, and Data Warehouses\", what is the first design decision when connecting an agent to a system of record?",
      "options": [
        "Which SDK or API endpoint to use for the connection",
        "Whether the call is a read or a write, because reads and writes need different identities, permissions, and guardrails",
        "Which cloud region the system runs in",
        "Whether to use a direct API or a managed platform",
        "Which LLM provider to route the tool call through"
      ],
      "correct_answer_index": 1,
      "explanation": "The lesson says to separate the read path from the write path first, because reading accepts a narrower identity and smaller blast radius while writing changes live business data. The connection mechanism is the small part; identity and reversibility come first."
    },
    {
      "question": "What does the lesson cite from Snowflake's managed MCP server documentation as the recommended way to expose warehouse data to an agent on the read path?",
      "options": [
        "Expose a raw SQL execution tool so the agent has full query freedom",
        "Expose the managed MCP server together with the SQL execution tool on the same server so they share permissions",
        "Expose a Cortex Agent as the client-facing tool, keeping the agent inside semantic views and verified queries, and isolate any direct SQL behind a separate least-privileged role",
        "Always disable all MCP tools and expose the warehouse through a direct REST API only",
        "Store the data in the agent's own vector store instead of querying the warehouse"
      ],
      "correct_answer_index": 2,
      "explanation": "The Snowflake docs recommend exposing a Cortex Agent as the governed client-facing tool, and warn that exposing the SQL execution tool on the same server lets a client bypass the agent's semantic views, verified queries, and orchestration. If direct SQL is required it should be on a separate server with a dedicated least-privileged role."
    },
    {
      "question": "According to the lesson, what is the key difference between the write path into a CRM and the read path into a data warehouse?",
      "options": [
        "The write path never needs a least-privileged role because writes are performed inside the CRM",
        "Both paths always use the same shared identity and grants",
        "The write path changes live business data, so it should prefer platform actions that apply business rules and put human confirmation on destructive or expensive operations, while reads stay read-only inside governed views",
        "The write path should always use a direct API while the read path must always use MCP",
        "The write path requires a higher recursion depth than the read path"
      ],
      "correct_answer_index": 2,
      "explanation": "The lesson stresses that a write is a commitment in shared state and should go through governed platform actions with a human confirmation at the blast-radius boundary, while the read path stays read-only inside the warehouse's own governance. The two paths use separate identities, separate grants, and separate guardrails."
    }
  ]
}