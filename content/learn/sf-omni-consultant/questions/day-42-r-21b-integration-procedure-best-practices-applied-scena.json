{
  "quizName": "sf-omni-consultant:lesson:day-42-r-21b-integration-procedure-best-practices-applied-scena",
  "title": "Lesson 42 - Integration Procedure Best Practices Applied: Scenario Walkthroughs",
  "description": "Discuss best practices for Integration Procedures.",
  "questions": [
    {
      "question": "In a read scenario, the client needs a filtered list from a REST endpoint. Where should the filter be applied for best performance?",
      "options": [
        "In the client component after the data arrives",
        "In the request query parameters so the endpoint returns only matching records",
        "In the Response action",
        "In the named credential"
      ],
      "correct_answer_index": 1,
      "explanation": "Filtering in the request query parameters lets the endpoint return only the matching records, minimizing payload and latency. Filtering in the client wastes bandwidth, and the Response action and named credential are not for filtering."
    },
    {
      "question": "A consultant is designing a write scenario where the IP creates a record in an external system. Why must the error path avoid blindly retrying the call?",
      "options": [
        "Retries are always slower",
        "The first call may have succeeded, so a retry can create a duplicate record",
        "Write operations never fail",
        "Retries are not supported for POST"
      ],
      "correct_answer_index": 1,
      "explanation": "A write is non-idempotent, so if the first call succeeded but the response was lost, a retry creates a duplicate. The error path must handle this per operation rather than retry blindly."
    },
    {
      "question": "In a multi-step scenario, the IP must fetch a token and then call a data endpoint with it. What is the correct design?",
      "options": [
        "Use one HTTP Action that does both",
        "Run a first HTTP Action to obtain the token, store it in a variable, and pass it to a second HTTP Action",
        "Put the token in the named credential and call once",
        "Skip the token and use basic auth"
      ],
      "correct_answer_index": 1,
      "explanation": "A custom multi-step flow needs a first HTTP Action to obtain the token, a variable to hold it, and a second HTTP Action that passes it to the data call. One action cannot do both, and the named credential handles standard flows, not this custom sequence."
    }
  ]
}
