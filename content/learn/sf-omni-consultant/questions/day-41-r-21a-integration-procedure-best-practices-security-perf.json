{
  "quizName": "sf-omni-consultant:lesson:day-41-r-21a-integration-procedure-best-practices-security-perf",
  "title": "Lesson 41 - Integration Procedure Best Practices: Security, Performance, and Error Handling",
  "description": "Discuss best practices for Integration Procedures.",
  "questions": [
    {
      "question": "A consultant wants to minimize the latency and API usage of an external call in an IP. What is the best practice?",
      "options": [
        "Call the endpoint multiple times to get all data",
        "Filter at the source and request only the fields the client needs",
        "Request the full payload and filter in the client",
        "Increase the timeout to the maximum"
      ],
      "correct_answer_index": 1,
      "explanation": "Filtering at the source and requesting only needed fields reduces payload size and external call latency, and it avoids consuming API limits. Calling repeatedly, requesting full payloads, or raising the timeout all make performance worse."
    },
    {
      "question": "Why should a consultant avoid automatically retrying a POST operation on timeout?",
      "options": [
        "Retries are always faster",
        "The first call may have succeeded, so a retry can create a duplicate record",
        "POST operations never time out",
        "Retries are not supported"
      ],
      "correct_answer_index": 1,
      "explanation": "A POST is non-idempotent, so if the first call succeeded but the response was lost, a retry creates a duplicate record. Retries must be considered per operation, and non-idempotent calls need special handling."
    },
    {
      "question": "Where should external endpoint URLs and secrets be stored for an Integration Procedure?",
      "options": [
        "In the IP's output variables",
        "In named credentials referenced by the HTTP Actions",
        "In a custom label",
        "In the Data Mapper Transform action"
      ],
      "correct_answer_index": 1,
      "explanation": "Named credentials are the security boundary that holds endpoints and secrets, and the HTTP Actions reference them. Output variables, custom labels, and the Data Mapper are not the place for connection details or secrets."
    }
  ]
}
