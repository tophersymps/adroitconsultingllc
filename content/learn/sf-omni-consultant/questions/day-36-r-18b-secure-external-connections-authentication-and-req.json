{
  "quizName": "sf-omni-consultant:lesson:day-36-r-18b-secure-external-connections-authentication-and-req",
  "title": "Lesson 36 - Secure External Connections: Authentication and Request/Response Mapping",
  "description": "Provide guidance at a high level on the configuration and management of HTTP Actions within an IP to securely connect to and process data from external REST/SOAP endpoints, including authentication and request/response mapping.",
  "questions": [
    {
      "question": "An external REST endpoint supports OAuth 2.0 with a client credentials grant. Where should the client ID and client secret be stored?",
      "options": [
        "In the HTTP Action's request body",
        "In the named credential",
        "In a custom label",
        "In the IP's output variables"
      ],
      "correct_answer_index": 1,
      "explanation": "The named credential is the security boundary that holds the client ID, client secret, token endpoint, and scopes, and it handles the token exchange. The request body carries data, not credentials. Custom labels and output variables are not for secrets."
    },
    {
      "question": "A consultant maps a nested JSON response directly into flat output variables and the client component shows empty fields. What is the correct fix?",
      "options": [
        "Change the HTTP method",
        "Add a Data Mapper Transform action to reshape the response",
        "Increase the timeout",
        "Switch to basic authentication"
      ],
      "correct_answer_index": 1,
      "explanation": "Nested JSON needs reshaping before it fits flat output variables, and the Data Mapper Transform action is the tool for that. The HTTP method, timeout, and auth protocol do not affect how the response is structured."
    },
    {
      "question": "Which authentication approach is the strongest choice when an external endpoint offers both OAuth 2.0 and basic authentication?",
      "options": [
        "Basic authentication, because it is simpler",
        "OAuth 2.0, because it uses token exchange and refresh instead of sending credentials on every call",
        "No authentication, because the endpoint is internal",
        "A custom header with a hard-coded API key"
      ],
      "correct_answer_index": 1,
      "explanation": "OAuth 2.0 exchanges credentials for a token and refreshes it, so the raw credentials are not sent on every request. Basic auth sends credentials each call and is more exposed. Skipping auth or hard-coding an API key is insecure."
    }
  ]
}
