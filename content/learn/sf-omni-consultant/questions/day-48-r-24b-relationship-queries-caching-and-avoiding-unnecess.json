{
  "quizName": "sf-omni-consultant:lesson:day-48-r-24b-relationship-queries-caching-and-avoiding-unnecess",
  "title": "Lesson 48 - Relationship Queries, Caching, and Avoiding Unnecessary Mapping",
  "description": "Describe the techniques to optimize Data Mapper performance, such as reducing the number of queries, efficient filtering, relationship queries, caching, and avoiding unnecessary data mapping.",
  "questions": [
    {
      "question": "A Data Mapper must read a parent record and its related child records. What is the most efficient approach?",
      "options": [
        "Query the parent, then query each child separately",
        "Use a relationship query that traverses the relationship in one call",
        "Use a Load to read the data",
        "Query only the children"
      ],
      "correct_answer_index": 1,
      "explanation": "A relationship query reads the parent and children together in one call, reducing the number of queries. Separate queries add reads, a Load writes, and querying only children misses the parent."
    },
    {
      "question": "A consultant needs to read data across related objects. Which Data Mapper type supports the relationship traversal?",
      "options": [
        "Turbo Extract",
        "Extract",
        "Load",
        "Transform"
      ],
      "correct_answer_index": 1,
      "explanation": "Extract supports the full SOQL feature set including relationship traversal. Turbo Extract has a narrower feature set, Load writes, and Transform does not query."
    },
    {
      "question": "The same reference data is needed multiple times in a flow and changes rarely. What is the best optimization?",
      "options": [
        "Re-query it each time",
        "Cache the result of the first read and reuse it",
        "Map it into every output",
        "Use a Load to store it"
      ],
      "correct_answer_index": 1,
      "explanation": "Caching the first read lets later steps reuse the data instead of re-querying. Re-querying repeats the cost, and the other options do not avoid the repeated read."
    }
  ]
}
